import { ContaEntradaBanco } from './conta-entrada-banco';

describe('ContaEntradaBanco', () => {
  it('should create an instance', () => {
    expect(new ContaEntradaBanco()).toBeTruthy();
  });
});
