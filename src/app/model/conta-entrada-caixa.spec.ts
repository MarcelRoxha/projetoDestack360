import { ContaEntradaCaixa } from './conta-entrada-caixa';

describe('ContaEntradaCaixa', () => {
  it('should create an instance', () => {
    expect(new ContaEntradaCaixa()).toBeTruthy();
  });
});
