import { ContaCadastradaFornecedor } from './conta-cadastrada-fornecedor';

describe('ContaCadastradaFornecedor', () => {
  it('should create an instance', () => {
    expect(new ContaCadastradaFornecedor()).toBeTruthy();
  });
});
